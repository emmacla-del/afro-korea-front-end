import 'package:flutter/material.dart';

class MyPoolsPage extends StatelessWidget {
  const MyPoolsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('My Pools - Coming Soon', style: TextStyle(fontSize: 24)),
    );
  }
}
